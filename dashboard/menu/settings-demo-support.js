(function(){
let installed=false;const labels={cash:'كاش',card_at_venue:'بطاقة عند المطعم',pay_on_delivery:'الدفع عند الاستلام'};
function inject(){if(typeof ops==='undefined'||!ops)return;const sec=document.getElementById('general');const card=sec?.querySelector('.card');if(!card)return;let box=document.getElementById('paymentMethodsBox');if(!box){box=document.createElement('div');box.id='paymentMethodsBox';box.className='card';box.style.marginTop='12px';card.appendChild(box)}const enabled=Array.isArray(ops.settings?.payment_methods)?ops.settings.payment_methods:['cash','card_at_venue','pay_on_delivery'];box.innerHTML='<b>طرق الدفع المفعّلة</b><div class="grid2" style="margin-top:8px">'+Object.entries(labels).map(([v,l])=>`<label class="card"><input type="checkbox" data-pay-method="${v}" ${enabled.includes(v)?'checked':''}> ${l}</label>`).join('')+'</div><div class="muted">Online Payment غير مفعّل في الديمو.</div>';const cards=[...document.querySelectorAll('#staffList .order-card')];(ops.staff||[]).forEach((s,i)=>{if(s.phone&&cards[i]&&!cards[i].querySelector('.demo-phone')){const d=document.createElement('div');d.className='order-meta demo-phone';d.textContent='الهاتف: '+s.phone;cards[i].querySelector('.order-head > div')?.appendChild(d)}})}
function install(){if(installed)return;if(typeof M==='undefined'||typeof ops==='undefined'){setTimeout(install,80);return}installed=true;const base=M.sb.rpc.bind(M.sb);M.sb.rpc=function(fn,args,opts){if(fn==='cfy_menu_save_operational_settings'&&document.querySelector('[data-pay-method]')){const pm=[...document.querySelectorAll('[data-pay-method]:checked')].map(x=>x.dataset.payMethod);return base(fn,{...args,p_data:{...(args?.p_data||{}),payment_methods:pm}},opts)}return base(fn,args,opts)};setInterval(inject,700)}install();
})();
