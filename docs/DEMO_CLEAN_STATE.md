# Demo Clean State

Before final Professional Menu acceptance, all existing test/demo client records were intentionally removed from the live CARDfy database at the user's request so a single planned demo tenant can be created from a clean state.

The new demo tenant is not created by this branch. Its content and commercial presentation should be defined separately, then created through the normal CARDfy Admin flow.
